
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pms
    username: postgres
    password: pasang2060
    driver-class-name: org.postgresql.Driver
  jpa:
    show_sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8899
